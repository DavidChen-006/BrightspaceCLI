
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Node / build
node_modules/
dist/
*.tgz
.env
.env.*

# Local BS_ROOT state directories used while testing (never commit session data)
.bs-root*/
bs-root*/
test/.tmp/
