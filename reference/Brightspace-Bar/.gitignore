node_modules/
# Without a trailing slash this also matches a node_modules SYMLINK, which is how
# experiment 7 borrows experiment 1's playwright install.
node_modules
brightspace-mcp-server/

# Swift build products — 158M of derived data
.build/
*.xcuserstate

**/artifacts/session.json
.env
.env.*
!.env.sample
!.env.example
.DS_Store
.claude/worktrees/
